<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
        <% if(userId === undefined){ %>
        <form method="POST" action="/login">
        <a> Login: <input type="text" name="username" placeholder="enter username" style="width: 300px; margin: 1em">  
          <input type="text" name="password" placeholder="enter password" style="width: 300px; margin: 1em">
          <button type="submit" class="btn btn-primary">Submit</button></a>
      </form>
      <% } else{ %>
        <form method="POST" action="/logout">
        <a class="nav-item">Logged in as: <%= userId.id %> <button type="submit" class="btn btn-primary">Logout</button></a></a>
        </form>
        <% } %>
      </div>
    </div>
  </nav>
</header>